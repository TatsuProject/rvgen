# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
build/
dist/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Local scratch / working artefacts (not checked in)
prompt.txt
out/
out_*/
/tmp_*/

# Claude Code project context — local-only tooling scaffold.
# The stable architectural reference lives under docs/; this file
# tracks ephemeral session status that doesn't belong in public git.
CLAUDE.md

# Porting research notes — internal SV-source digests used while
# extending the generator. The polished public summary lives at
# docs/research/comparison-and-next-steps.md; these root-level notes
# are working scaffolding only.
/research/

# Virtual environments
.venv/
venv/
env/
site/
